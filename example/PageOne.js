import React from 'react';

export default (props) => {
    return (
        <div style={{
            background: 'pink'
        }}>
            This is the &lt;PageOne /&gt; component
        </div>
    );
};
